<div class="container">
 <div class="row">
  <div class="mx-auto">
   <h4 class= "mt-3 mb-5">投稿一覧</h4>
    <% @posts.each do |post| %>
    <div><%= link_to post_path(post.id) do %>
         <%= post.title %>
         <% end %>
    </div>


    <% end %>
  </div>
 </div>
</div>
